# requirements.in - List of Python packages your project needs
# These will be installed when you run "make install" or "pip install -r requirements.in"

# AWS SDK for Python
boto3==1.34.0

# Utilities for AWS Lambda (makes Lambda development easier)
aws-lambda-powertools==2.40.0

# Testing framework
pytest==8.0.0

# Test coverage reporting
pytest-cov==4.1.0

# Mocking library for tests
pytest-mock==3.12.0

# Environment variables management
python-dotenv==1.0.0

# Type hints (optional but helpful)
typing-extensions==4.9.0